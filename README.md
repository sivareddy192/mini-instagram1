SetUp MongoDB, Express, Bcryptjs, Cloudinary, Connect-flash, Cookie-parser, dotenv, ejs, jsonwebtoken, multer, nodemon and tailwindcss
i created config, models, routes, uploads, views, and server.js
After i add create a feed route using pagimation showing only 4 posts per page.
implement setup logic where uses can only update and delete my post's only
By using ejs styling and testing the flow.
